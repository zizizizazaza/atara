# Atara — Impeccable Design Context

## Design Context

### Users
两类用户共用一套站点，但分区承接：
- **首页 / Landing**：面向决策者与财务（采购、CFO、产品负责人）。需要对比、数字、合规与成本证据。避免堆砌专业术语，但保留具体性（金额、延迟、商户名）。
- **Dashboard / Authorize**：面向构建 agent 的工程师。可以使用 `trace_id`、API 字段、sats、routing 等术语；信息密度可以高，密集表格、终端字体、单调色块都可接受。
- 共同任务：让 AI agent 在全球商户安全付款，并能审计每一笔交易。
- 阅读情境：白天屏幕、桌面优先、键盘可用。

### Brand Personality
**精密 · 克制 · 略带前卫**

主基调 80% 偏向 *Linear / Vercel / Payman* 的工程师审美 —— 冷静、结构化、可信、用数字说话；再加 20% 的实验感与态度，让产品在一众 "AI agent 工具" 里有辨识度（编号卡、手作几何图、noise grain、微动效都属此类）。

情绪目标：让读者感到 "这家公司知道自己在做什么"，而不是 "这家公司很会卖东西"。

### Aesthetic Direction
- **基底**：保留当前 peach 暖色 (`#E8D5C4`) 单一主题，不引入 dark mode；warmth 是辨识度。
- **配色锚点**：`peach` 背景 / `core #1A151C` 文字 / `glow #8CB6E8` 强调 / `cool #E2E2E6` 次级面板。强调色稀缺使用 —— 仅在 hover、active、关键数字、状态点上出现。
- **字体**：Space Grotesk（标题/正文）+ Space Mono（数字、`trace_id`、终端日志、编号）。Mono 是 "工程师血统" 的视觉信号，要在 hero / dashboard / pricing 中保持密度。
- **质感**：noise grain + 浅 grid bg + 手绘几何插画（pillar art、use-case art）。避免 glassmorphism、霓虹渐变、3D 球体、AI sparkle 图标。
- **参考**：Payman 的 Execution Trace 面板、Tempo 的编号卡、Kite 的三层叙事、Linear 的密度与字号收敛。
- **反参考**（明确避免）：
  1. Stripe-clone 式 SaaS 落地页（蓝色 hero + 产品截图 + 客户 logo 排排坐）。
  2. Web3 浮夸风（霓虹渐变、玻璃拟态、过度发光、token mascot）。
  3. 极简到没信息（一句话 + 一个按钮 + 90% 留白）—— Atara 需要展示具体数字。
  4. AI 产品通用调（紫色渐变、sparkle 图标、抽象 AI 插画）。

### Design Principles
1. **具体压过抽象** —— 凡能用金额、延迟、商户名、`trace_id` 代替形容词的地方，都要替换。文案与 UI 都遵循。
2. **mono 是骨架，不是装饰** —— Space Mono 用于一切可被引用的数据（金额、ID、状态、时间）。不要拿它做标题。
3. **强调色稀缺** —— `glow #8CB6E8` 只在 hover、active、CTA、关键数字上出现。一屏内出现两次就过载。
4. **节奏优先于对称** —— Use Case、Pillars、Dashboard 卡片避免同等权重铺陈；用尺寸/留白制造主次。
5. **动效服务信息** —— 微动效（uc-card、pillar-art、execution trace）必须传达状态变化或引导视线，不做纯装饰旋转。所有动效在 `prefers-reduced-motion` 下关闭。
6. **密度可以高，但层级必须清晰** —— Dashboard 允许高密度，但字号必须收敛到 3 档以内（0.625 / 0.75 / 0.875rem），padding 用 4/6/8 阶。

### Accessibility
- 颜色对比：`core` on `peach` 已满足 WCAG AA。`glow` on `peach` 仅用于装饰与 hover，不承担正文。
- `prefers-reduced-motion` 已在 use-case、pillar、loka 卡片中处理；新增动效必须遵循同一模式。
- 键盘焦点：所有 `.action-btn` 与表单元素需有可见 focus ring（待补 —— `outline: 2px solid #8CB6E8`）。
